{
  "name": "@almostjacked/health-mcp-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": { "fflate": "^0.8.2" },
  "devDependencies": { "esbuild": "^0.25.0", "typescript": "^5.5.0", "vitest": "^2.0.0" }
}
