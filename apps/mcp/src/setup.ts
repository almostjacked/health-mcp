// Stub: the interactive setup wizard lands in a follow-up task (creates the
// Supabase project, runs setup.sql, and wires up the MCP client config).
console.log("setup wizard: coming in this release");
process.exit(0);

export {};
